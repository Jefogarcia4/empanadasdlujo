// Valor fijo del domicilio (COP). Se suma al total de todo pedido con productos.
export const DELIVERY_FEE = 12000;

// Paquetes acumulados (sin contar combos) a partir de los cuales todo el
// carrito se repricia a mayorista.
export const WHOLESALE_THRESHOLD = 10;

// Compra mínima de la tienda, en paquetes combinables entre referencias.
export const MIN_PACKAGES = 2;
